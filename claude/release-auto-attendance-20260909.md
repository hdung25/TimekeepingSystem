# Automatic teacher attendance — 09 September 2026

Owner explicitly requested removing the preview/apply gate and supplied BẢNG LƯƠNG TG!AR4 as the authoritative old-mode attendance formula. This authorization supersedes the default opt-in rule for criterion I. Calculation is read-only until the normal payroll save; it never automatically publishes a payslip or writes attendance.

Old mode now calculates criterion I on every report/modal calculation using full-month teaching chips and explicit VP/VDX/VKP evidence. Unreported absence is VDX. Preserve exact Excel 50/65/>64.99 boundaries and priority, including the no-absence reward below 50 hours. New mode remains hours × entered monthly rate, including zero. The large discretionary-policy panel is replaced with an automatic result and a single rate input for new mode. Criteria II–X, notes, advances, class rates, separate adjustments and TT settings are preserved.

Every new saved result retains previous criterion I rows and calculation inputs in teacherAttendancePolicy/teacherAttendanceHistory. Existing manual criterion I is preserved in that history when first recalculated/saved; previously published and received payloads stay immutable until the existing revision workflow. Staff and senior roles cannot author policy/rate saves. The shared report payload applies the same automatic row even when GV is the inactive role in a dual-role report. Display filters never narrow the monthly policy inputs.

Validation: full npm suite passed; 585 independent Excel-formula combinations plus fractions/zero/unreported/office exclusion; full Chrome/Auth/Firestore emulator payroll suite passed (including prior overtime scenario, filters, payslips, export, history and receipt); targeted automatic-policy UI suite passed after adding main-save coverage. Verified compact old/new panels visually. No workbook was edited or uploaded, and no production salary records were migrated.

Production: same verified ha-huy-dungs-projects/timekeeping-system project and timekeeping-system-tawny.vercel.app alias. Update report/policy/editor URLs to 20260909-auto-attendance-v2 and cache v166. Prior deployment for rollback: dpl_C9tvFKZKfWvsykqUcm1CzwcRPtMv (scheduled overtime fix). The separate Vy 13/08 source correction already committed successfully with backup and audit uyen-vy-scheduled-overtime-20260909-v1; raw timestamps, existing approved 15 minutes, schedule and all other source fields verified unchanged.
